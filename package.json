{
  "name": "folio-resume-builder",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "description": "A private, Markdown-first resume builder",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@pdf-lib/fontkit": "1.1.1",
    "pdf-lib": "1.17.1"
  }
}

